# Copyright 2025 The KED Authors. All Rights Reserved.
# SPDX-License-Identifier: MIT

from typing import Optional


# TODO: add return type
def get_embedding(kernel_name: str, measure_name: str, kernel_config: Optional[dict], measure_config: Optional[dict]):
    """Constructs the kernel embedding from the given configurations.

    :param kernel_name:
    :param measure_name:
    :param kernel_config:
    :param measure_config:
    :return:
    """

    # TODO get kernel and measure instance and create kernelembedding instance
    if kernel_name == "":
        pass

